package org.example

class ConnectionPollProto {
    // what i want to do!?: Implement a simple connection pool using a Bounded Blocking Queue
    /**
     * - create a thread pool which limit connectios to a resource
     * - create clients which access this resources concurrently
     * - what happens without threadpool
     * - how to make them connect through the threadpool and limit the number of connections
     * - simulate concurrent access to the resources!!
     *
     * build all this from raw java code (but use stdlib datatypes. thats ok)
     */

}